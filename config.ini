; Common settings only. Omitted advanced options use safe defaults.
; For every budget value, 0 means unlimited.

[debate]
min_counter_rounds = 4
max_counter_rounds = 10

[codex]
model = gpt-5.6-sol
reasoning_effort = max
web_search = live

[paths]
project_root = .

[budget]
max_model_calls = 0
max_wall_minutes = 0
max_total_tokens = 0
max_estimated_cost_usd = 0

[adjudication]
; Publishes clearly labeled agent agreement without an independent review.
mode = none
